version: '3.8'

services:
  semantic-search-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: semantic-search-app
    ports:
      - "12100:7860"
    environment:
      - NVIDIA_API_KEY=${NVIDIA_API_KEY}  # Load this key from .env or manually add the secret
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}  # Load this key from .env or manually add the secret
      - CHROMA_DIR=${CHROMA_DIR}          # Load this key from .env or manually add the setting
    restart: unless-stopped

